{% extends "HCocktailBundle::layout.html.twig" %}

{% block main %}

    <h1>Cocktail ?</h1>
	
	{{ form_start(form) }}
	
		<div class="label-form">
			{{ form_row(form.color) }}
		</div>
		<div class="label-form">
			{{ form_row(form.age) }}
		</div>
		<div class="label-form">
			{{ form_row(form.langage) }}
		</div>

	{{ form_end(form) }}

{% endblock %}
