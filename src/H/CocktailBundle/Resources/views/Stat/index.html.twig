{% extends "HCocktailBundle::layout.html.twig" %}

{% block main %}
    <h1>Stat list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Approved</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('stat_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.approved }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('stat_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('stat_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('stat_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
